Update HANDOFF.md for the next session (CLAUDE.md section 11). Rewrite the top "READ FIRST" block using `templates/HANDOFF.template.md`: how they are, what moved today, applied and sent with dates, what is waiting on their go (file paths), facts corrected, and "next, in order" with tomorrow's single most important thing first. Move the previous top block below as a dated reference block. Pull applied/sent facts from `npx tsx src/cli.ts pipeline` and `npx tsx src/cli.ts contacts`. Confirm the file path when done. $ARGUMENTS
