Build an interview prep card for the company/round in the arguments and save it to `prep/<company>.md` using `templates/prep-card.md` (CLAUDE.md section 9).

Pull the JD (stored posting via `show <id>` or the URL), the posted comp band, the company's stated interview process and values if they publish them, and anything the user pasted from the recruiter's email. Map their proof points from data/profile.json to the requirements, one number each. Write the tight answers, three questions to ask, the honest gap, and the plan (one hour the night before for a screen; a recorded rehearsal day for team rounds).

Return the two things they must not forget and the path. Arguments: $ARGUMENTS
